<Window x:Class="WpfLoggingApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="WPF Logging App" Height="600" Width="800"
        MinHeight="400" MinWidth="600"
        WindowStyle="None" AllowsTransparency="False"
        Background="{StaticResource WindowBackgroundBrush}" UseLayoutRounding="True">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="28" GlassFrameThickness="0" ResizeBorderThickness="6" UseAeroCaptionButtons="False" CornerRadius="8" />
    </shell:WindowChrome.WindowChrome>

    <Border Background="{StaticResource WindowBackgroundBrush}" CornerRadius="8" BorderBrush="{StaticResource ControlBorderBrush}" BorderThickness="1" Padding="0" SnapsToDevicePixels="True">
        <Grid UseLayoutRounding="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title bar (drag to move, double-click to maximize/restore) -->
            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="WPF Logging App" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{StaticResource ControlForegroundBrush}" FontWeight="SemiBold"/>

                <!-- Custom window control buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button x:Name="MinimizeButton" Style="{StaticResource WindowControlButtonStyle}" Click="MinimizeButton_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Viewbox Width="12" Height="12">
                            <Canvas Width="10" Height="10">
                                <Path x:Name="MinimizeIcon" Stroke="{StaticResource ControlForegroundBrush}" StrokeThickness="1.5" Data="M1,6 L9,6" StrokeStartLineCap="Square" StrokeEndLineCap="Square"/>
                            </Canvas>
                        </Viewbox>
                    </Button>

                    <Button x:Name="MaximizeRestoreButton" Style="{StaticResource WindowControlButtonStyle}" Click="MaximizeRestoreButton_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Viewbox Width="12" Height="12">
                            <Canvas Width="10" Height="10">
                                <Path x:Name="MaximizeIcon" Stroke="{StaticResource ControlForegroundBrush}" StrokeThickness="1.2" Data="M1,1 L9,1 L9,9 L1,9 Z" />
                            </Canvas>
                        </Viewbox>
                    </Button>

                    <Button x:Name="CloseButton" Style="{StaticResource WindowCloseButtonStyle}" Click="CloseButton_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Viewbox Width="12" Height="12">
                            <Canvas Width="10" Height="10">
                                <Path x:Name="CloseIcon" Stroke="{StaticResource ControlForegroundBrush}" StrokeThickness="1.5" Data="M2,2 L8,8 M8,2 L2,8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                            </Canvas>
                        </Viewbox>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Buttons Panel -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
                <Button Style="{StaticResource RoundedButtonStyle}" Name="StartButton" Content="Start" Width="100" Height="30" Margin="0,0,10,0" Click="StartButton_Click"/>
                <Button Style="{StaticResource RoundedButtonStyle}" Name="StopButton" Content="Stop" Width="100" Height="30" Click="StopButton_Click"/>
            </StackPanel>

            <!-- Console Logging Area -->
            <Border Grid.Row="2" Background="{StaticResource ControlBackgroundBrush}" Margin="10,0,10,10" BorderBrush="{StaticResource ControlBorderBrush}" BorderThickness="1" CornerRadius="8" SnapsToDevicePixels="True">
                <ScrollViewer Name="LogScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <TextBox Name="LogTextBox" 
                             Background="{StaticResource ControlBackgroundBrush}" 
                             Foreground="{StaticResource LogForegroundBrush}" 
                             FontFamily="Courier New"
                             FontSize="12"
                             IsReadOnly="True"
                             BorderThickness="0"
                             TextWrapping="NoWrap"
                             VerticalScrollBarVisibility="Disabled"
                             HorizontalScrollBarVisibility="Disabled"/>
                </ScrollViewer>
            </Border>

        </Grid>
    </Border>
</Window>
